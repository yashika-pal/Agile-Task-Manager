server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_focus?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: yashika
  jpa:
    hibernate:
      ddl-auto: update # beginner-friendly; schema.sql also provided
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

app:
  cors:
    allowed-origins: "http://localhost:4200"
